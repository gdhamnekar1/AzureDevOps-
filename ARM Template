 01: Sign-in on
 https://portal.azure.com/
 

02: Install Azure CLI
Install [Azure CLI) https://docs.microsoft.com/en-us/cli/azure/install-azure-cli-windows?tabs-az
 Azure Version
-az version
Azure CLI Login
-az login
List Subscriptions
- az account list
Set Specific Subscription (if we have multiple subscriptions)
-az account set -subscription="SUBSCRIPTION_ID"

03: Install Visual Studio Code
Install [VS Code] https://code.visualstudio.com/download

04: Install ARM templates plugin

-Azure Resource Manager (ARM) Tools

05: Create ARM template with .json extension and select ARM template langauge

06: JQ Installation on GIT Bash

-curl -L -o /usr/bin/jq.exe https://github.com/stedolan/jq/releases/latest/download/jq-win64.exe
-jq --version


Commnds to run the ARM Teplates
1.  az deployment group create --resource-group <RGNamne> arm-vscode --template-file web-app.json --parameters web-app-parametors.json
2.  az deployment group validate --resource-group arm-vscode --template-file web-app.json --parameters web-app-parameters.json
3. az deployment group create --resource-group vinu9-rg  --template-file ARM_Web-App/web-app3.json --parameters ARM_Web-App/web-app-parameters.json
4. az group delete --resource-group <RGName> #to delete the rg
5. to delete entire deplyment
    $ az resource delete \
    > --resource-group devopsmela-rg \
    >--name <devopsmela-dev>\
    >--resource-type "copy n paste from .json in RG"


6. what if or confirm with what if = in tereform we use terraform plan 
